services:
  - type: web
    name: healthyvenus-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port 8501 --server.address 0.0.0.0
    envVars:
      - key: OPENAI_API_KEY
        scope: run
        sync: false
